# pm-publish-outputs

This project captures snapshots of the CLI outputs when running publishing commands in `pnpm`, `npm` and `yarn`.

The snapshots are in [the snapshot file](./src/__snapshots__/test.test.ts.snap).
